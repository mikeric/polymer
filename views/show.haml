%h1= @polyrhythm.title
%p= "Resolves at #{@polyrhythm.resolve/@polyrhythm.patterns[0].time} bars of #{@polyrhythm.patterns[0].time}/4"

- @polyrhythm.patterns.each do |p|
  %span
    = p.pattern * (@polyrhythm.resolve/p.time)